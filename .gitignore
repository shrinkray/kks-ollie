#---------------------------
# WordPress general
# @date 2024-1-2
#---------------------------
/index.php

/node_modules
#---------------------------
# Composer
#---------------------------
/vendor
.env
.env.*
!.env.example
auth.json

# Mac OS X
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# Linux
.directory

# dist/ is committed (built assets). If files never show under Source Control:
#   1) Open this theme folder as the repo root (kks-ollie), not the whole Local/site tree.
#   2) Run: git check-ignore -v dist/koolkatscience.css — if matched, relax ~/.gitignore_global or use git add -f dist/
